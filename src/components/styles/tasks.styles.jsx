import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        padding: 0,
        backgroundColor: "white",
        width: '100%',
        flex: 1,
        alignItems:'center',
        paddingTop: 10
    }
});

export  {styles};